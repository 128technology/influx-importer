build:
  binary: influx-importer
  goos:
    - darwin
    - linux
  goarch:
    - amd64
  ldflags: -s -w -X main.build={{.Version}}
archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS
    linux: Tux
  files:
    - drum-roll.licence.txt